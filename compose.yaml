services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=skilldatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
    volumes:
      - ./pgdata:/var/lib/postgresql/data
